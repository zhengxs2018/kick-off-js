---
description: 助手约束文件（SKILL / RULE 等）的编写规范：分类、自描述、去废话、目录结构
alwaysApply: true
enabled: true
---

# 约束文件编写规范

适用于所有助手约束文件：RULE、SKILL 及同类指令文件。

## 分类
- 内容分三类，标题明示：**强制规则**（必须遵守）、**辅助规则**（验证/检查类，非强制写法约束）、**项目信息**（当前事实，如目录结构、构建命令、协作约定）。
- 项目信息只列事实，不夹带「如何遵守」的辅助说明。

## 规则文字自描述
- 每条规则直接写明作用对象 + 约束 + 例外，使文字本身可独立理解，不依赖前缀标记（如 `[结果]`/`[过程]`/`[验证]`）。
- 写法示例：全局、局部与类的私有变量名以 `_` 开头；不限制 JS 类的 `#` 私有变量。
- 禁止用方括号标签替代清晰表述。

## 去除废话
- 禁止写辅助提示词：如「AI 启动时已加载故会遵守」「请看 xxx 文件」「本节点为 xxx 契约」等解释性文字——它们干扰 skill 与重构任务，不增加约束。
- 禁止提示性注释与复述内容的说明；只保留「为何」级约束理由。
- 来源引用（如「来自 Clean Code」）不写；规则本身即约束。

## 目录结构

- 一规则一文件夹，规则文件固定命名为 `RULE.mdc`；禁止在一个文件夹内放多个规则文件，禁止多级嵌套规则文件夹。
- frontmatter 用 `description` / `alwaysApply` / `enabled`；
- 文件用 Markdown（`.mdc`）格式。

## 编辑后的自我核对闭环

修改任何约束文件后，必须将「全文符合规范」作为完成条件，不得把「响应了当前指令」当终止条件。

- 改完立即重读整个文件，逐条对照本规范的分类、自描述、去废话、目录结构四节，而非只回看刚改的片段。
- 接到「排查并修复」类指令时，先通读全文建问题清单，再批量修复，最后通读确认零遗漏，一轮交付。
- 语义不清的口语化指令先简短澄清再动手，不臆测。
- 交付时在回复中写明核对结论（如「已逐条核对 N 条，无残留说明性文字」），让用户一眼可知是否真做完。
