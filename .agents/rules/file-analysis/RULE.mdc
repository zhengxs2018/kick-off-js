---
description: 分析项目/文件时的强制前置规则——先用 IDE 工具（list_dir/search_file/search_content/read_file/read_lints）建文件地图、查字节数、猜入口模块、顺带取诊断，再从入口开始分析；结论落 .output/。凡涉及「读文件做分析、理解代码、review、解读模块」的任务都必须遵守。
alwaysApply: true
---

# 文件分析强制规则（可复用分析）

任何「分析项目 / 分析文件 / 解读代码 / review / 理解模块」类任务，在动手读内容之前，**必须先走下面的流程**，确保分析可复用、不重复、可追溯。

分析全程**只用 IDE 内部工具，禁用 shell 命令**（`ls`/`find`/`grep`/`cat`/`stat` 等）。可用工具与用途：

- `list_dir` — 列目录/文件清单，建立结构地图。
- `search_file` — 按文件名/通配符定位入口与特定文件。
- `search_content` — 按内容/正则搜定义、引用、调用关系（替代手动通读找落点）。
- `read_file` — 读文件内容，用 `offset`/`limit` 按行区间分段，避免超大文件撑爆上下文。
- `read_lints` — 读取 linter 诊断，分析时顺带发现已知问题。

## 1. 用 IDE 工具建立文件地图，猜测源码目录

- 用 `list_dir` 建立顶层目录地图，按命名（如 `src/`、`packages/`、`samples/`、`scripts/`、`docs/`）**猜测源码相关目录**。
- 需要定位具体文件时用 `search_file`（如通配 `*.entry.ts`、`index.ts`）。
- 探索应**避开以 `.` 开头的文件夹**（如 `.agents`、`.codebuddy` 等 IDE 设置目录不承载业务结构信息）。
- **首次探索目录白名单**：仅允许访问以下范围，其余目录不在首轮探索内：
  - `docs`、`src/`、`scripts/`、包目录（`packages/`、`samples/`）
  - 根目录文件（`DESIGN.md`、`package.json`、`README.md` 等）
  - 需触及白名单外目录（如 `node_modules`、构建配置、测试）时，先在记忆/文档里记录动机，再单独申请。
- **文件大小的常规范式**：干净 TS 单文件健康区间约 **100–400 行（5–20KB）**；超 **600 行（约 30KB）** 已偏大需警惕拆分；超 **1000 行 / 50KB** 即判定为需分组拆分的超大文件。

## 2. 查文件字节数，超大必须分组 + 拆分

- 用 `search_file` 找到实体文件路径后，在终端执行 `ls -l` / `stat` 拿到字节数（IDE 工具不返回文件尺寸，字节数仍需 shell 查询）。
- 超过阈值（默认 **50KB / 约 1000 行**）即视为「超大文件」：
  - **不得一次性全量读入分析**。
  - 必须**按逻辑分组 + 文件拆分**（如按行区间 `L1-200`、按导出块、按模块）分批分析，用 `read_file` 的 `offset`/`limit` 限定区间。
- 小文件（≤20KB / 约 400 行）可直接整体分析。

## 3. 根据文件名猜测最顶层入口模块

- 依据目录结构与命名（如 `index.ts`、`main.ts`、`app.ts`、`*.entry.ts`、包根 `package.json` 的 `main`/`exports`）**猜测最顶层入口模块**。
- 用 `search_file` 精确确认入口存在，再用 `read_file` 读其前 20 行验证导入/导出签名，再决定下一步细化范围。

## 4. 从入口模块开始文件分析（两遍扫描）

**第一遍：快速扫描（不全部读取）**

- 以入口模块为根，**自顶向下**按导入链展开，而非随机通读。
- 每个文件**只读前 20 行**（`read_file` 设 `limit: 20`）：覆盖文件头 import 区 + 顶层 `export` 声明（函数/类/接口/常量）。
- 仅凭这 20 行即可判定文件作用——**看导入知道依赖哪些模块，看导出知道对外提供什么能力**；TS 约定 import 在上、导出紧随其后，模块"是什么"基本在此一屏。
- 若前 20 行仍是 import 链（依赖极多的文件），最多补读到 **前 40–60 行** 看首个导出块，不再深入。
- 用 `search_content` 检索符号的调用方/定义方，缩小需打开的文件范围，避免盲目读文件。
- 分析时并行调用 `read_lints` 收集该文件/目录的已知诊断，作为分析结论的一部分。
- **逐文件即时落盘草稿（提速关键）**：每扫完一个文件，**立即**写一份草稿到 `docs/codegraph/drafts/<相对路径>.md`（路径同名映射，如 `src/foo.ts` → `docs/codegraph/drafts/src/foo.md`）。草稿内容只需：模块名 | 路径 | 一句话职责（基于 import/export 推断）| 关键导出 | 依赖 | `read_lints` 问题 | 置信度（高/中/低）。**先写后扫下一个**，禁止攒一批再写——目的是让后续会话/子 agent 直接读草稿而非重扫，避免重复消耗。若草稿已存在且文件未改动，跳过重扫直接复用。

**第二遍：按需全文读取**

- 仅在以下情况才全文读取：需修改该文件、定位具体 bug、理解某算法实现，或首遍发现它是被大量 import 的核心枢纽。
- 全文读取同样用 `read_file` 的 `offset`/`limit` 按逻辑分组分段进行，超大文件禁止一次性灌入。

**第三遍：跨文件关系验证（非全文读取）**

- 第三遍不重读代码，而是用 `search_content` 在文件间**证实前两遍的推断**：追某个导出的真实调用方、确认循环依赖、核对架构假设是否成立。
- 触发条件：要产出"依赖关系 / 模块关系"类文档时；或前两遍结论相互矛盾需裁决时。
- 这遍服务于下方文档体系——文档的本质是把第一、二遍的猜测，用第三遍的搜索坐实。

- 主代理应先用第一遍扫描列文件、缩范围后再分派子 agent；不要把整棵树原样丢给一个子 agent，按页面/功能域切成小批。

### 方法论依据

上述三遍对应实证研究的 **hierarchical understanding flow（分层理解流）**（Gao et al., arXiv:2504.04553）：专业代码审计员按 全局层（结构/业务流/调用流）→ 局部层（模块/变量/依赖顺序）→ 细节层（代码段确认）渐进理解，而非直接读源码。研究显示分层渐进比"读工具回复/直接读源码"效率高得多（阅读时间降约 79%），**新手尤受益于起点引导，避免无目的探索**。第一、二、三遍即分别对齐全局→局部→细节三层；分层文档体系对应"技术地图 + 个人文档 + 以教促学"（GitHub Engineering, 2025）。

## 5. 草稿是分层文档的输入（聚合而非重扫）

第 4 节的逐文件草稿（`.output/codegraph/drafts/`）是下面分层文档的**唯一数据源**，汇总阶段直接读取草稿聚合，不重新扫描源码：

**A. 项目分析总结（单个 md）** — `.output/codegraph/README.md` 或 `.output/codegraph/<项目名>.md`
- 顶层视角：目录树结构、各层职责一句话概述、文件间关系图（可用文本树/箭头表示依赖方向）、关键技术栈与约定。
- 从草稿目录聚合生成，是所有细分文档的索引入口，新会话先看这一份即可定位。

**B. 详细依赖分析表（单个 md）** — `.output/codegraph/dependencies.md`
- 表格化呈现：模块路径 | 导入了谁 | 被谁导入 | 耦合度（高/中/低）| 备注。
- 由第三遍关系验证坐实，避免基于猜测的依赖描述。

**C. 分层分析表（多个 md）** — `.output/codegraph/<分层>.md`
- 按代码角色拆分为数份，每份聚焦一类：
  - `.output/codegraph/core.md` — 基础/核心层（被广泛依赖、无业务语义的底座模块）。
  - `.output/codegraph/feature.md` — 功能层（承载具体业务/玩法的模块）。
  - 其余按需追加（如 `engine.md`、`store.md`、`ui.md`）。
- 每份表格：模块名 | 路径 | 一句话职责 | 关键导出 | 关联文件。从草稿按角色归类聚合。

**D. 以教促学（反思校验）** — 文档完成后用四问自检，缺口回到对应草稿/遍次补分析：
- 能否用几句话描述该系统？
- 它如何与相邻模块/系统交互？
- 分析中最意外/反直觉之处是什么？
- 哪些部分仍不清晰（列待办，不强行编造）？

**通用约束**
- 该目录是过程数据的一部分，与代码同等重要，**不得只写在对话回答里**。
- 记忆条目应精简：模块名 + 路径 + 一句话职责 + 关联文件（及 `read_lints` 发现的已知问题），不贴大段代码。
- 文档之间用相对链接互指（A 引 B/C，B/C 回指 A），形成可导航网络而非孤岛。
- **断点续扫**：分析中断后重跑，先查 `drafts/` 已有草稿，仅补扫缺失/改动的文件，不全量重来。

## 6. 分析任务的实现红线

- 分析/探索阶段**禁止直接写业务代码**：代码只在设计确认后的实现阶段产出。
- 涉及**大型任务**（多模块改动、新功能、架构调整）时，**必须先使用 `docs/` 做需求与架构设计**（PRD / 决策草案 / 系统设计），经确认后才进入实现；不允许在探索/设计阶段直接写码。
